import { Box, Typography } from '@mui/material';

const NotFound = () => (
  <Box>
    <Typography variant="h3" display="flex" alignItems="center" justifyContent="center" height="calc(100vh - 64px)">
      404 Not Found
    </Typography>
  </Box>
);

export default NotFound;